# Hard rename to Moonlight

Moonlight is the canonical name for the behavior comparison system. The earlier accidental name had leaked into environment variables, scripts, Docker files, UI text, docs, and storage paths, and would otherwise continue to split the project language; we decided to remove it from public surfaces without compatibility aliases so future APIs, docs, and operational tooling all use one vocabulary.
